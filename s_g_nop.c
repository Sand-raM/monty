#include "monty.h"
/**
 * s_g_nop - a func that doesn't do anything
 * @hd: the head of the stack
 * @num_ln: count num lines
 * Return: nothing to be returned
 */
void s_g_nop(stack_t **hd, unsigned int num_ln)
{
	(void) num_ln;
	(void) hd;
}
