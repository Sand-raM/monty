#include "monty.h"

/**
 * s_g_stack - a func that prints the top
 * @hd: head of the stack
 * @num_ln: counts num lines
 * Return: nothing to be returned
*/
void s_g_stack(stack_t **hd, unsigned int num_ln)
{
	(void)hd;
	(void)num_ln;
	bus.flg_chng = 0;
}
